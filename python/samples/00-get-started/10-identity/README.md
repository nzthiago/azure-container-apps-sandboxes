# Identity (Python)

```bash
pip install -r requirements.txt
python managed_identity.py
```

Creates a temporary sandbox group called `mi-demo-<random>` and deletes
it at the end. Does not touch the shared samples group.
