fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# Azure plumbing the receiver actually uses.
azure-identity>=1.15
# Sandbox SDK — pinned to the same early-access wheel the rest of the
# repo uses, so the receiver image and the local-dev `python/` runner
# always boot sandboxes against the same SDK shape.
https://github.com/microsoft/azure-container-apps/releases/download/python-sdk-v0.1.0b1-early-access/azure_containerapps_sandbox-0.1.0b1-py3-none-any.whl
