# 05-data-processing - Data pipelines

> **Coming soon.** This scenario is a placeholder. Track progress in [samples/sandboxes/README.md](../../README.md).

Producer/consumer pipelines on shared AzureBlob volumes. One sandbox writes results, another reads and aggregates — both see the same mount. Composes guides 04 (volumes) + 01 (sandboxes).

