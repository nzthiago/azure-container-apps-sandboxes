# 03-code-interpreter - LLM code interpreter

> **Coming soon.** This scenario is a placeholder. Track progress in [samples/sandboxes/README.md](../../README.md).

LLM-driven code execution. Generate code, run it in a sandbox, observe stdout/stderr, feed results back to the model, iterate. The same loop OpenAI's Code Interpreter ships, on your own data. Composes guides 01 (sandboxes) + 07 (files).

